
场馆预约系统 - 后端技术笔记

项目名称：场馆预约系统
作者：sbw
技术栈：Python 3.14 + Flask 2.3 + SQLite 3
版本：v1.0

一、项目概述

本项目是一个基于 Flask 框架的场馆预约系统后端服务，提供用户管理、场地管理、预约/取消/签到、智能推荐、全局优化等完整功能。系统采用 RESTful API 风格设计，统一返回 JSON 格式数据，与前端通过 HTTP 协议通信。

二、Flask 路由设计

2.1 路由总览

路由 方法 功能 权限
/api/register POST 用户注册 公开
/api/login POST 用户登录 公开
/api/courts GET 获取场地列表 公开
/api/book POST 创建预约 公开
/api/cancel POST 取消预约 公开
/api/checkin POST 签到 公开
/api/my_reservations GET 查询我的预约 公开
/api/recommend GET 推荐场地 公开
/api/users GET 获取用户列表 管理员
/api/optimize POST 全局优化 管理员
/ GET 根路径（接口列表） 公开

2.2 路由设计规范

1. RESTful 风格

· 使用名词复数表示资源：/api/courts、/api/users
· 用 HTTP 方法表示操作：GET（查询）、POST（创建）
· 状态码语义化：200（成功）、400（参数错误）、401（未授权）、403（禁止）、404（不存在）、409（冲突）、500（服务器错误）

2. 统一返回格式

所有 API 返回统一格式：

```json
{
  "success": true,
  "data": { ... },
  "msg": "操作成功"
}
```

失败时：

```json
{
  "success": false,
  "data": null,
  "msg": "具体错误原因"
}
```

3. 权限控制

· /api/users 和 /api/optimize 仅管理员可访问
· 前端请求时携带 Authorization: user_xxx header
· 后端通过 check_admin() 函数验证 token

4. 并发安全

· 预约接口使用 BEGIN IMMEDIATE 显式加锁
· 同一场地同一时段只允许一人预约成功
· 冲突时返回 409 状态码和明确提示

三、数据库表结构

3.1 数据库设计概述

使用 SQLite 作为数据库，包含三张表：users（用户表）、courts（场地表）、reservations（预约表）。外键约束保证数据一致性。

3.2 users 表（用户表）

存储用户账号信息。

```sql
CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT NOT NULL UNIQUE,
    password TEXT NOT NULL,
    role TEXT DEFAULT '普通用户'
);
```

字段 类型 说明
id INTEGER 主键，自增
username TEXT 用户名，唯一
password TEXT 密码（目前明文存储）
role TEXT 角色：普通用户 / 管理员

示例数据：

```sql
INSERT INTO users (username, password, role) VALUES 
('zhangsan', '123456', '普通用户'),
('admin', 'admin123', '管理员');
```

3.3 courts 表（场地表）

存储场地信息。

```sql
CREATE TABLE IF NOT EXISTS courts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    location TEXT,
    status TEXT DEFAULT '空闲'
);
```

字段 类型 说明
id INTEGER 主键，自增
name TEXT 场地名称
type TEXT 场地类型（羽毛球/乒乓球/篮球）
location TEXT 场地位置（如"一楼A区"）
status TEXT 状态：空闲/预约中/占用/维护

示例数据：

```sql
INSERT INTO courts (name, type, location, status) VALUES 
('A馆-羽毛球场 01', '羽毛球', '一楼A区', '空闲'),
('A馆-羽毛球场 02', '羽毛球', '一楼B区', '空闲');
```

3.4 reservations 表（预约表）

存储预约记录。

```sql
CREATE TABLE IF NOT EXISTS reservations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    court_id INTEGER NOT NULL,
    date TEXT NOT NULL,
    start_time TEXT NOT NULL,
    end_time TEXT NOT NULL,
    status TEXT DEFAULT '预约中',
    created_at TEXT,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (court_id) REFERENCES courts(id)
);
```

字段 类型 说明
id INTEGER 主键，自增
user_id INTEGER 用户ID，外键关联 users.id
court_id INTEGER 场地ID，外键关联 courts.id
date TEXT 预约日期（YYYY-MM-DD）
start_time TEXT 开始时间（HH:MM）
end_time TEXT 结束时间（HH:MM）
status TEXT 状态：预约中/已取消/已使用/已超时
created_at TEXT 创建时间（用于超时释放）

示例数据：

```sql
INSERT INTO reservations (user_id, court_id, date, start_time, end_time, status, created_at) 
VALUES (1, 1, '2026-08-19', '18:00', '20:00', '预约中', datetime('now', 'localtime'));
```

3.5 表关系图

```
users (1) ─────< (N) reservations >───── (N) courts (1)
  │                    │                        │
  │                    │                        │
  └────── id ─────────┘                        │
       用户ID                           场地ID ←┘
```

四、核心 API 示例

4.1 用户登录

请求：

```bash
curl -X POST http://127.0.0.1:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"zhangsan","password":"123456"}'
```

返回：

```json
{
  "success": true,
  "data": {
    "token": "user_1",
    "user_id": 1,
    "role": "普通用户"
  },
  "msg": "登录成功"
}
```

4.2 获取场地列表

请求：

```bash
curl http://127.0.0.1:5000/api/courts
```

返回：

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "A馆-羽毛球场 01",
      "type": "羽毛球",
      "location": "一楼A区",
      "status": "空闲"
    }
  ],
  "msg": "操作成功"
}
```

4.3 创建预约

请求：

```bash
curl -X POST http://127.0.0.1:5000/api/book \
  -H "Content-Type: application/json" \
  -d '{"user_id":1,"court_id":1,"date":"2026-08-19","start_time":"18:00","end_time":"20:00"}'
```

返回（成功）：

```json
{
  "success": true,
  "data": {"reservation_id": 10},
  "msg": "预约成功"
}
```

返回（冲突）：

```json
{
  "success": false,
  "data": null,
  "msg": "该场地刚被他人预约，请刷新后重试"
}
```

4.4 推荐场地

请求：

```bash
curl http://127.0.0.1:5000/api/recommend?user_id=1
```

返回：

```json
{
  "success": true,
  "data": {
    "preferred_type": "羽毛球",
    "preferred_time": "18:00",
    "preferred_floor": 1,
    "recommendations": [
      {
        "id": 1,
        "name": "A馆-羽毛球场 01",
        "type": "羽毛球",
        "location": "一楼A区",
        "status": "空闲",
        "score": 100,
        "reason": "类型匹配 +50；同楼层 +30；时段偏好 +20"
      }
    ]
  },
  "msg": "操作成功"
}
```

五、业务逻辑说明

5.1 预约流程

1. 前端发送预约请求，包含 user_id、court_id、date、start_time、end_time
2. 后端校验参数格式（时间、日期格式）
3. 校验用户和场地是否存在
4. 并发安全：执行 BEGIN IMMEDIATE 获取数据库写入锁
5. 在锁内检查时段冲突
6. 无冲突则插入预约记录，更新场地状态为"预约中"
7. 提交事务，释放锁，返回预约ID

5.2 超时释放机制

· 每次查询场地列表时触发
· 检查所有"预约中"的记录
· 若 created_at 超过 15 分钟未签到，自动改为"已超时"
· 同时释放对应场地，状态恢复为"空闲"
· 防止有人占着场地不签到

5.3 推荐算法

评分维度：

维度 分值 说明
类型匹配 50分 用户偏好类型与场地类型一致
楼层匹配 30分 用户常去楼层与场地楼层一致
时段匹配 20分 用户常约时段与场地时段一致

总分 100 分，按分数从高到低排序返回前 5 个场地。

5.4 模拟退火优化

· 仅管理员可执行 /api/optimize
· 调用 zrd 的 sa_duiqi.py 模拟退火算法
· 输入：场地列表 + 用户列表
· 输出：最优分配方案（plan）及匹配率
· 超时限制 2.5 秒，保证页面不卡死

六、总结

本项目后端采用 Flask + SQLite 轻量级架构，实现了完整的场馆预约系统 API：

·  11 个 RESTful API 接口，覆盖注册、登录、预约、签到、推荐、优化等全部功能
·  统一返回格式，前端对接方便
·  并发安全，使用数据库锁解决抢约冲突
·  超时释放机制，自动释放 15 分钟未签到的预约
·  智能推荐算法，基于用户历史偏好加权推荐
·  权限控制，管理员接口受保护
·  模拟退火优化，实现全局最优分配

代码结构清晰、注释完整，团队协作时易于理解和维护。