# 前端技术笔记

> 项目：体育馆智能预约系统  
> 作者：zjy  
> 日期：2026-08-19  
> 技术栈：HTML5 + CSS3 + JavaScript + Bootstrap 5

---

## 一、技术选型说明

本项目前端采用**纯原生技术栈**（HTML/CSS/JS），未引入 Vue/React 等重型框架，主要考虑以下两点：

1. **项目规模适中**：仅有 4 个核心页面（登录、场地看板、我的预约、管理员后台），引入框架反而增加构建复杂度。
2. **快速迭代需求**：暑假 40 天密集开发，原生技术零构建成本，保存即刷新，调试效率高。

UI 框架选用 **Bootstrap 5**，主要利用其栅格系统实现 D23 的移动端适配任务。以下将按页面维度记录设计思路与踩坑经历。

---

## 二、页面设计与实现

### 2.1 login.html —— 登录与双通道注册

**设计思路**：  
采用居中卡片布局，背景使用淡紫色渐变提升视觉层次。核心功能是**双通道注册**：普通用户直接注册，管理员需输入邀请码（`admin666`）提升权限。

**关键代码 —— 登录/注册模式切换**：

```javascript
toggleLink.addEventListener("click", function() {
    if (isTransitioning) {
        message.textContent = "请稍等...";
        message.style.color = "#999";
        return;
    }

    isLogin = !isLogin;
    if (isLogin) {
        formTitle.textContent = "🔐 登录";
        submitBtn.textContent = "登录";
        toggleText.textContent = "还没有账号？";
        toggleLink.textContent = "去注册";
        document.getElementById("inviteCodeDiv").style.display = "none";
    } else {
        formTitle.textContent = "📝 注册";
        submitBtn.textContent = "注册";
        toggleText.textContent = "已有账号？";
        toggleLink.textContent = "去登录";
        document.getElementById("inviteCodeDiv").style.display = "block";
    }
    message.textContent = "";
});
```

**踩坑记录**：  
早期版本将 `role` 写死在后端，导致注册后无法区分管理员。后改为后端根据邀请码 `admin666` 自动判断 `role`，前端仅负责传值，解决了管理员注册通道的问题。

---

### 2.2 courts.html —— 场地看板与推荐系统

**设计思路**：  
页面分为三大模块：顶部导航栏、中间推荐区、底部场地表格。推荐区使用渐变紫背景 + 白色卡片突出个性化内容；场地表格采用 Bootstrap 的 `table-hover` + 响应式 `table-responsive`。

**关键代码 —— 推荐卡片动态渲染**：

```javascript
fetch(API_BASE + "/api/recommend?user_id=" + userId)
    .then(function(r) { return r.json(); })
    .then(function(data) {
        var recData = data.data || data;
        if (!recData.recommendations || recData.recommendations.length === 0) {
            container.innerHTML = '<div class="text-center py-4">暂无推荐，先去预约几个场地吧</div>';
            return;
        }
        // 动态生成 Bootstrap 栅格列
        for (var i = 0; i < Math.min(3, recData.recommendations.length); i++) {
            var rec = recData.recommendations[i];
            var col = document.createElement("div");
            col.className = "col-md-4";  // 桌面端3列，手机端自动堆叠
            col.innerHTML = '...';
            cardsDiv.appendChild(col);
        }
    })
    .catch(function(e) {
        // 推荐加载失败，静默处理，避免打断用户主流程
    });
```

**踩坑记录 —— 从 my.html 取消预约后状态不同步**：  
用户在我的预约页取消订单后，返回场地看板，表格中该场地仍显示"预约中"。原因是浏览器使用缓存页面，未重新请求数据。

**解决方案**：监听 `visibilitychange` 事件，页面重新可见时自动刷新场地列表：

```javascript
document.addEventListener("visibilitychange", function() {
    if (!document.hidden) {
        loadCourts();
        loadRecommendations();
    }
});
```

---

### 2.3 my.html —— 预约管理与签到

**设计思路**：  
按状态分三个区块（进行中/已完成/已取消），每个预约卡片显示场地名称、时段、状态标签。签到按钮仅在"进行中"状态显示，已签到后变为"已签到"灰色标签。

**踩坑记录 —— 签到后取消按钮消失**：  
早期逻辑将"已签到"视为终态，直接隐藏取消按钮。但实际场景中用户可能误签到，需要保留取消入口。后改为：签到后仍显示取消按钮，但取消时后端校验时间规则。

---

### 2.4 admin.html —— 全局优化结果展示

**设计思路**：  
单页应用风格，一个大按钮触发优化，下方动态渲染分配卡片。使用 CSS Grid（`grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))`）实现自适应卡片布局，匹配结果按分数分三色（完美/偏好/一般）。

**关键代码 —— 真实名称替换 ID**：

```javascript
// 建立 ID→对象 映射，渲染时用真实名称
window._courtsMap = {};
for (var i = 0; i < courts.length; i++) {
    window._courtsMap[courts[i].id] = courts[i];
}

var user = window._usersMap[p.user_id];
var court = window._courtsMap[p.court_id];
var userName = user ? user.username : ('用户 #' + p.user_id);
```

---

## 三、移动端适配（D23 任务）

**技术方案**：  
- 全局添加 `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
- 使用 Bootstrap 5 栅格系统（`col-md-4` → 手机端自动变为单列）
- 按钮和输入框增加 `btn-sm`、`form-control` 等触控友好类名
- 表格外包 `table-responsive` 容器，横向溢出可滑动

**踩坑记录**：  
admin.html 是后期新增的页面，初期漏掉了 `viewport` 标签，导致手机打开时字体极小、按钮无法点击。D29 代码整理时发现并补上。

---

## 四、前后端联调踩坑汇总

### 4.1 API_BASE 配置 —— 从 localhost 到局域网 IP

开发阶段前端用 `http://127.0.0.1:5000` 调后端，但 D23 手机测试时发现手机无法访问电脑 localhost。后将 `API_BASE` 改为局域网 IP：

```javascript
var API_BASE = "http://192.168.1.35:5000";
```

注意：该 IP 随网络环境变化，演示前需重新确认。

### 4.2 跨域与 CORS

Flask 后端已配置 `flask-cors`，但早期出现过前端请求带自定义 Header（如 `Authorization`）时触发预检请求（OPTIONS）失败的问题。后端通过 `CORS(app, supports_credentials=True)` 解决。

### 4.3 未登录状态拦截

D20 任务要求"未登录用户访问我的预约自动跳登录页"。实现方式是在各页面顶部统一检查 `localStorage`：

```javascript
var token = localStorage.getItem("token");
if (!token) {
    window.location.href = "login.html";
}
```

但 courts.html 作为首页需保留浏览权限，因此仅拦截预约操作（点击场地时跳转登录），而非页面级拦截。

---

## 五、代码规范与工程化

### 5.1 D29 代码整理要点

- **删除调试代码**：清理全部 `console.log`、测试用的 `alert`
- **头部注释**：每个文件顶部添加文件名、作者、功能说明
- **命名统一**：全组统一为下划线命名法（`load_courts` 而非 `loadCourts`，但前端 JS 仍保留驼峰以区分前后端风格）
- **临时脚本清理**：删除 `_fix_*.py`、`_test_*.py` 等辅助脚本

### 5.2 文件结构

```
FlaskProject/
├── login.html      # 登录注册
├── courts.html     # 场地看板 + 推荐
├── my.html         # 我的预约
├── admin.html      # 管理员优化
├── app.py          # Flask 后端
├── sa_duiqi.py     # 模拟退火算法
├── sa_demo.py      # 演示配置
├── create_demo_db.py  # 演示数据初始化
└── court_booking.db   # SQLite 数据库
```

---

## 六、总结

本项目前端虽然技术栈简单，但覆盖了**响应式布局**、**异步数据交互**、**状态管理**（localStorage）、**移动端适配**等核心前端技能。最大的收获是理解了"工程化意识"——即使不写 Vue，也要保持代码整洁、注释完整、调试代码及时清理，这对团队协作至关重要。


