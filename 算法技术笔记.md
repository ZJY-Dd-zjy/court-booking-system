场馆预约系统 - 模拟退火算法技术笔记

作者：zrd
算法名称：模拟退火算法（Simulated Annealing, SA）
应用场景：场馆预约系统中的用户-场地最优分配问题
版本：v2.0（带时间维度和场地唯一性约束）

一、算法概述

1.1 为什么选择模拟退火？

场馆预约分配是一个典型的组合优化问题：给定 N 个用户和 M 个场地，每个用户有偏好类型，每个场地有类型和可用时间段，需要找到一种分配方案，使得匹配的用户数最多。

这是一个 NP-hard 问题，当用户数和场地数较大时，穷举搜索不可行。模拟退火算法通过概率突跳机制，能够在可接受的时间内找到近似最优解。

1.2 算法来源

模拟退火算法来源于对固体退火过程的模拟：

· 加热 → 固体内部粒子变得无序，能量升高
· 缓慢冷却 → 粒子逐渐有序，能量降低
· 最终 → 达到能量最低的稳定状态

类比到优化问题中：

· 目标函数值 相当于 能量
· 最优解 相当于 能量最低状态
· 温度参数 控制算法接受差解的概率

二、核心原理

2.1 Metropolis 准则

模拟退火的核心是 Metropolis 准则：在温度 T 下，从当前解 i 移动到新解 j 时，接受新解的概率为：

```
P(accept) = 1,                        if f(j) >= f(i)
P(accept) = exp((f(j) - f(i)) / T),   if f(j) < f(i)
```

其中：

· f(i) 和 f(j) 分别是当前解和新解的目标函数值
· T 是当前温度

通俗解释：

1. 如果新解更好（目标函数值更高），一定接受
2. 如果新解更差，以一定概率接受，概率随温度降低而减小
3. 高温时容易接受差解（探索能力强），低温时只接受好解（局部精化）

2.2 退火过程

算法从初始温度 T₀ 开始，逐步降温，每个温度下进行多次迭代：

```
T₀ → α·T₀ → α²·T₀ → ... → T_min
```

其中 α 是降温系数（通常取 0.85~0.99）。

2.3 算法伪代码

```
1. 初始化：生成初始解 s，计算目标函数值 f(s)
2. 设置：初始温度 T₀，终止温度 T_min，降温系数 α
3. while T > T_min:
4.     for i in range(iterations_per_temp):
5.         生成邻域解 s'（随机交换一个用户的场地分配）
6.         计算 Δ = f(s') - f(s)
7.         if Δ > 0 或 random() < exp(Δ / T):
8.             s = s'
9.         更新最优解
10.    T = T * α
11. 返回最优解
```

三、目标函数设计

3.1 问题建模

输入：

· 用户集合 U = {u₁, u₂, ..., uₙ}，每个用户有偏好类型列表
· 场地集合 C = {c₁, c₂, ..., cₘ}，每个场地有类型和可用时间段

约束条件：

1. 每个用户最多分配到一个场地
2. 每个场地在每个时间段最多分配给一个用户（唯一性约束）

优化目标：
最大化匹配的用户数量，即用户偏好类型与分配场地类型一致。

3.2 目标函数公式

```
F(solution) = match_score - violation_penalty - no_pref_penalty
```

其中：

1. 匹配得分（match_score）：

```
match_score = 10 × type_match_count + 5 × time_match_count
```

· type_match_count：用户偏好类型与场地类型一致的数量
· time_match_count：用户偏好时段与场地时段一致的数量

2. 违规惩罚（violation_penalty）：

```
violation_penalty = 10 × Σ(max(0, usage[court] - 1)) + 50 × I(violation_exists)
```

· 每个场地超员分配一个用户，惩罚 10 分
· 如果存在任何违规，额外惩罚 50 分

3. 无偏好惩罚（no_pref_penalty）：

```
no_pref_penalty = 2 × (有偏好但未匹配的用户数)
```

3.3 匹配率计算

```
match_rate = matched_users / total_users
```

其中：

· matched_users：偏好类型与分配场地类型一致的用户数
· total_users：总用户数

四、参数选择实验

4.1 关键参数

参数 含义 取值范围
T₀ 初始温度 60~120
α 降温系数 0.85~0.95
iterations_per_temp 每温度迭代次数 30~100
max_iterations 最大迭代次数 10000~500000

4.2 实验设置

测试场景： 16个用户，5个场地，每个场地2个时间段
重复次数： 每组参数重复运行 5 次取平均值
评估指标： 匹配率、运行时间、违规数

4.3 实验结果

实验1：初始温度 T₀ 的影响（固定 α=0.88, iter=60）

T₀ 匹配率 运行时间(秒) 违规数
60 82.5% 0.45 0.2
80 87.5% 0.48 0.0
100 85.0% 0.51 0.0
120 83.7% 0.53 0.1

结论： T₀=80 时匹配率最高，且无违规。

实验2：降温系数 α 的影响（固定 T₀=80, iter=60）

α 匹配率 运行时间(秒) 违规数
0.85 83.7% 0.40 0.1
0.88 87.5% 0.48 0.0
0.90 86.2% 0.55 0.0
0.92 84.1% 0.62 0.2

结论： α=0.88 时匹配率最高。

实验3：每温度迭代次数的影响（固定 T₀=80, α=0.88）

iter 匹配率 运行时间(秒) 违规数
30 81.2% 0.30 0.3
60 87.5% 0.48 0.0
100 87.5% 0.72 0.0
150 88.1% 1.05 0.0

结论： iter=60 时匹配率已接近最优，继续增加时间收益递减。

4.4 最终推荐参数

```
initial_temperature = 80
min_temperature = 0.1
alpha = 0.88
iterations_per_temp = 60
max_iterations = 50000
max_no_improvement = 1000
```

五、收敛曲线分析

5.1 典型收敛曲线

```
适应度
  ^
  |                      ____-----
  |                 ____/
  |            ____/
  |        ___/
  |    ___/
  |___/_____________________________ 迭代次数
  |
  0     500    1000    1500    2000
```

5.2 曲线解读

阶段 迭代次数 特征 说明
探索阶段 0~300 适应度快速上升 高温，接受差解概率高
过渡阶段 300~800 适应度缓慢上升 温度降低，开始精化
收敛阶段 800~1500 适应度趋于稳定 低温，只接受好解

5.3 关键指标

· 最优适应度： 通常在迭代 1500 次后稳定
· 匹配率： 可达到 85~95%（取决于数据类型）
· 违规数： 最终收敛为 0（满足场地唯一性约束）
· 运行时间： 约 0.3~0.8 秒（满足 2.5 秒超时要求）

六、与穷举搜索对比

指标 穷举搜索 模拟退火
时间复杂度 O(mⁿ) O(n·iter)
用户数 5 0.1 秒 0.1 秒
用户数 10 不可行（数十亿） 0.3 秒
用户数 50 完全不可行 0.8 秒
最优性保证 100% ~90%
适用场景 小规模（n < 8） 任意规模

七、总结

7.1 核心要点

1. 模拟退火通过概率突跳机制，有效避免了陷入局部最优
2. 目标函数包含匹配得分、违规惩罚和无偏好惩罚，平衡了优化目标和约束条件
3. 参数调优后，算法在 0.5 秒内达到 85%+ 匹配率，且满足场地唯一性约束
4. 收敛曲线显示，算法在 1500 次迭代后趋于稳定

7.2 改进方向

1. 动态调整迭代次数：根据解的质量自适应停止
2. 多种邻域结构：不仅交换用户，还可交换时间段
3. 并行加速：多线程同时探索不同区域
4. 集成学习：用历史数据预测最优参数组合