# 🎯 场馆预约系统 - 演示脚本（Demo v1.0）

> **演示账号**：`demo` / `demo`  
> **管理员账号**：`admin` / `admin123`  
> **其他测试账号**：`zhangsan` / `123456`，`lisi` / `123456`  
> **目标**：3 分钟内完成核心功能演示

---

## 一、环境准备

1. 启动后端：`python app.py`（确保运行在 `http://192.168.1.41:5000` 或 `http://127.0.0.1:5000`）
2. 浏览器访问：`http://192.168.1.41:5000/login.html`
3. 确保数据库已重置为演示数据（运行 `python create_demo_db.py`）

---

## 二、演示流程（按顺序执行）

### Step 1：普通用户登录（30秒）
- **操作**：在登录页输入用户名 `demo`，密码 `demo`，点击【登录】
- **预期结果**：
  - 跳转至 `courts.html`（场地看板）
  - 页面右上角显示 "欢迎，demo"
  - 场地列表中有 5 个场地，部分显示"预约中"

### Step 2：查看"为你推荐"（30秒）
- **操作**：在场地看板页面向下滚动，查看"为你推荐"区域
- **预期结果**：
  - 显示推荐场地及权重分数（如"类型匹配 +50"、"同楼层 +30"）
  - 因为 demo 账号有历史预约记录，会基于偏好推荐

### Step 3：预约场地（30秒）
- **操作**：
  1. 选择一个状态为"空闲"的场地（如 A馆-羽毛球场 02）
  2. 点击【预约】按钮
  3. 选择日期（今天或明天）、开始时间（如 18:00）、结束时间（如 20:00）
  4. 点击【确认预约】
- **预期结果**：
  - 弹窗提示"预约成功"
  - 该场地状态变为"预约中"

### Step 4：签到（20秒）
- **操作**：
  1. 在刚刚预约的场地上点击【签到】按钮
  2. 或前往"我的预约"页面进行签到
- **预期结果**：
  - 弹窗提示"签到成功"
  - 场地状态变为"占用"

### Step 5：查看"我的预约"（20秒）
- **操作**：点击页面顶部【我的预约】导航，或访问 `my.html`
- **预期结果**：
  - 列表显示 demo 账号的所有预约记录（含历史记录和当前记录）
  - 状态列显示：已使用、预约中、占用等
  - 已签到的预约显示"占用"，未签到的显示"预约中"

### Step 6：管理员登录并执行优化（60秒）
- **操作**：
  1. 退出当前账号，或新开浏览器标签页访问 `login.html`
  2. 使用管理员账号登录：`admin` / `admin123`
  3. 页面会自动跳转至 `admin.html`（管理员控制台）
  4. 在管理员页面查看：
     - 用户列表（zhangsan、lisi、demo 等）
     - 场地状态
     - 匹配率统计（理论匹配率 vs 实际匹配率）
  5. 点击【执行优化】按钮
- **预期结果**：
  - 按钮变为"优化中..."，2~3秒后返回结果
  - 显示优化后的分配方案
  - "理论匹配率"和"实际匹配率"有具体数值
  - 运行时间 < 3 秒

### Step 7：演示算法效果对比（可选，30秒）
- **操作**：在管理员页面查看优化前后的匹配率变化
- **预期结果**：
  - 如果用户预约偏好与场地类型匹配，匹配率应较高
  - 优化算法会给出更合理的分配建议

---

## 三、异常场景测试（加分项）

| 场景 | 操作 | 预期提示 |
|------|------|----------|
| 未登录访问"我的预约" | 直接访问 `my.html` 而不登录 | 自动跳转回登录页 |
| 预约冲突 | 两个用户同时预约同一时段同一场地 | 后提交的用户看到"该场地刚被他人预约" |
| 超时释放 | 预约后 15 分钟内不签到 | 系统自动释放，状态变回"空闲" |
| 权限不足 | 普通用户访问 `admin.html` | 页面应限制或提示权限不足 |

---

## 四、演示数据说明

演示数据库 `court_booking.db` 预置数据：

- **用户**（4人）：
  - zhangsan（普通用户，偏好羽毛球）
  - lisi（普通用户，偏好乒乓球）
  - admin（管理员）
  - demo（普通用户，演示专用，有历史记录）

- **场地**（5个）：
  - A馆-羽毛球场 01（一楼A区）
  - A馆-羽毛球场 02（一楼B区）
  - B馆-乒乓球场 01（二楼C区）
  - B馆-乒乓球场 02（二楼D区）
  - C馆-篮球场 01（三楼E区）

- **历史预约**（8条）：
  - zhangsan：3条（2条已使用，1条预约中）
  - lisi：3条（2条已使用，1条预约中）
  - demo：2条（均已使用，用于推荐算法演示）

---

## 五、常见问题

**Q：为什么推荐算法显示 demo 有偏好？**  
A：因为演示数据中为 demo 预置了 2 条历史预约记录，系统会根据历史记录分析偏好。

**Q：优化算法的匹配率是怎么算的？**  
A：理论匹配率是算法计算的最优方案匹配率；实际匹配率是当前数据库中真实预约记录里，用户偏好类型与实际预约场地类型的匹配比例。

**Q：演示时场地状态不对怎么办？**  
A：运行 `python create_demo_db.py` 重置数据库即可恢复初始状态。

---

*本脚本对应 D27（8月16日）Demo v1.0 准备任务*
